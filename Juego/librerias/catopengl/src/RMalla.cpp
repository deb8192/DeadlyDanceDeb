#include "RMalla.hpp"

RMalla::RMalla()
{
    tipo = 'M';
    objetos = 0;
    mallas = 0;
}

void RMalla::Remove()
{

}

bool RMalla::CargarRecurso(const char * _ruta)
{

    //carga los datos con assimp, mete el nummero de datos que tiene en objetos, numeros de mallas que tiene en mallas
    return false;
}

/* datos * RMalla::GetMalla()
{
    return datos;
}
*/